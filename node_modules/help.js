var words = [
["T", "R", "E", "E", "H", "O", "U", "S", "E"],
  ["J","A","V","A","S","C","R","I","P","T"],
  ["W","E","B","D","E","S","I","G","N"],
  ["E","D","U","C","A","T","I","O","N"],
  ["C","H","O","C","O","L","A","T","E"],
  ["G","E","R","M","A","N","Y"]
]
var random = Math.floor((Math.random()*(words.length-1))); 

var newWord = words[random]; // the word to guess will be chosen from the array above
var newWord_Hidden = new Array(newWord.length);
var wrongLetter = 0;

// every letter in the word is symbolized by an underscore in the guessfield
for (var i = 0; i < newWord_Hidden.length; i++){
	newWord_Hidden[i] = "_ ";
}

// prints the guessfield


//checks if the the letter provided by the user matches one or more of the letters in the word
var checkLetter = function(){
	var f = document.rateformular; 
	var b = f.elements["rateletter"]; 
	var letter = b.value; // the letter provided by the user
	letter = letter.toUpperCase();
	for (var i = 0; i < newWord.length; i++){
		if(newWord[i] === letter){
			newWord_Hidden
        [i] = letter + " ";
			var hit = true;
		}
	b.value = "";
	}
	
	//deletes the guessfield and replaces it with the new one
	var guessField = document.getElementById("guessField");
	guessField.innerHTML=""; 
	printnewWord_Hidden
();
	
	// if a guessed letter is not in the word, the letter will be put on the "wrong letters"-list and hangman grows
	if(!hit){
		var geratenelettern = document.getElementById("geratenelettern");
		var letter = document.createTextNode(" " + letter);
		geratenelettern.appendChild(letter); 
	 wrongLetter++;
		var hangman = document.getElementById("hangman");
    hangman.src = "http://www.writteninpencil.de/Projekte/Hangman/hangman" + wrongLetter + ".png";
	}
	
	//checks if all letters have been found
	var winner = true;
	for (var i = 0; i < newWord_Hidden
.length; i++){
		if(newWord_Hidden
    [i] === "_ "){
			winner = false;
		}
	}
	if(winner){
		window.alert("You win!");
	}
	
	//once you got six wrong letters, you lose
	if wrongLetter === 6){
		window.alert("Uh...I guess you're dead now.");
	}
}

function init(){
	printnewWord_Hidden
();
}

window.onload = init;